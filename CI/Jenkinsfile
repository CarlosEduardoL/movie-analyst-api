pipeline {
    agent {
        docker { image 'node:14-alpine' }
    }
    stages {
        stage("test"){
            steps {
                sh 'npm install'
                sh 'npm test'
            }
        }
        stage("build"){
            steps {
                junit './test-results.xml'
            }
        }
    }
}